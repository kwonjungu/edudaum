# B08 체험 설계 — 버튼 값과 조건

## 0. 만들까 말까

**만들지 않는다.**

기준값 양쪽(아홉 번·열 번)을 눌러 결과가 갈리는지 보는 일은 활동3·4에서 실물 버튼으로 직접 한다.
웹에 같은 버튼과 같은 기준값을 두면 실물 시험을 앞질러 답을 보여 주는 꼴이 된다.

덧붙임: 기준값을 10에서 5로 바꾸어 견주는 대목만 떼어 웹으로 옮길 수도 있으나, 그 대목 역시
과정안 활동4 안에 실물 실습으로 들어 있다. 15~16차시에는 웹이 들어갈 빈자리가 없다.
