# B15 체험 설계 — 가르쳐 보기

2026. 9. 17. 판정을 뒤집었다. 앞선 판정문은 9절에 남겨 둔다.

## 0. 만들까 말까

**만든다.** 단, 앞 판정이 막았던 자리(사례 카드 여덟 장의 판정)는 그대로 막아 둔다.

- **웹이 아니면 못 하는가.** 활동1은 "만보기가 따르는 명령 순서"와 "손동작 분류가 예시에서
  무엇을 배우는지"를 견주라고 한다. **가르쳐지는 쪽과 가르쳐지지 않는 쪽을 같은 기기에서
  한 번씩 해 보는 일**은 종이로 안 된다. 그림으로 보면 둘 다 그냥 그림이다.
- **값을 바꾸면 결과가 바뀌는가.** 예시를 더 담으면 판정 막대가 눈앞에서 달라진다.
  흔들리는 칸은 아무리 해도 달라지지 않는다. 이 **달라짐의 차이**가 이 차시의 기준이다.

**여전히 만들지 않는 것.** 사례 카드 여덟 장의 판정, 인공지능인지 아닌지의 결론,
"더 알아봐야 함"의 처리. 화면은 어느 쪽이 인공지능이라고 **말하지 않는다.**
화면에 `카드 여덟 장은 종이에서 나눠요`를 상시로 둔다.

---

## 1. 과정안 어디에 붙는가

- 블록 B15, 1~2차시. **1차시 활동1 한가운데**, 두 사례를 기준 칸에 나누어 적기 **직전**에 4~6분.
- 활동1의 셋째·넷째 줄("만보기에 새로운 걸음 방식을 가르칠 수 있는지",
  "손동작 분류가 예시 사진에서 무엇을 배우는지")을 말 대신 손으로 한 번 해 보는 자리다.
- 활동2(사례 카드 여덟 장)는 종이로 그대로 한다. 화면은 활동2에 들어가지 않는다.
- 5학년 B11을 한 학급이면 **윗 칸이 그때 그것**이라는 것을 바로 알아본다.

---

## 2. 한 문장

윗 칸은 흔들어도 **가르칠 곳이 없고**, 아랫 칸은 예시를 담을수록 **판정이 달라진다**.

---

## 3. 화면 (1화면, 위아래 두 칸)

| 칸 | 보이는 것 | 학생이 하는 것 | 조작 요소 |
|---|---|---|---|
| 위 `흔들면 세는 칸` | 수 하나, 한 줄 `여기엔 담기 칸이 없어요` | 태블릿을 흔든다 | **없다**(몸으로만) |
| 아래 `예시로 배우는 칸` | 카메라 그림, 막대 둘(가·나), 담은 장수 | `가 담기` `나 담기`를 누른다 | 담기 2 + 다시 하기 1 |

- 위 칸에 버튼이 **하나도 없는 것**이 이 화면의 알맹이다. 비워 둔 것이지 빠뜨린 것이 아니다.
- 아래 칸의 막대는 예시가 한쪽에만 있으면 움직이지 않는다. 양쪽에 하나씩 담긴 뒤부터 움직인다.
- 세로 800px 안에 두 칸이 다 들어온다. 좁으면 카메라 그림을 줄인다.

---

## 4. 조작과 값

- 조절하는 값: **담은 예시 장수** 하나. `가`와 `나` 각각 0~20장.
- 주된 조작: `가 담기` / `나 담기`. 한 번 누르면 한 장.
- 보조 조작: `다시 하기`(담은 예시를 모두 버린다. 윗 칸의 수도 0으로).
- 이름은 `가` `나`로 고정한다. 학생이 이름을 적는 칸을 두지 않는다.

**윗 칸의 흔들림 세기**는 B11과 똑같은 규칙을 쓴다(임계 16, 잠금 250ms, 상한 99).
같은 규칙을 다시 쓰는 것이 의도다. **윗 칸은 무엇을 해도 세는 방식이 달라지지 않는다.**

**미니 이미지 모델** (기기 안에서만 돈다)
- 카메라 그림 한가운데 정사각형을 24×24로 줄인다.
- 특징 20개: 평균 R·G·B(3), 평균 밝기(1), 4×4 칸의 밝기(16).
- 판정: 담은 예시 전부와의 가중 유클리드 거리에서 가까운 3개를 뽑아, 가까울수록 큰 값을
  클래스별로 더한 뒤 둘의 합이 1이 되게 나눈다. 그 둘을 막대 길이로 쓴다.
- 0.3초마다 한 번만 판정한다.

**열어 두는 딴짓.** 한쪽만 잔뜩 담으면 무엇을 비춰도 그쪽으로 기운다. **막지 않는다.**
활동2의 "센서가 달렸다는 까닭만으로"를 짚기 전에, 예시가 치우치면 판정도 치우친다는 것을
먼저 손으로 만나 두는 편이 낫다.

---

## 5. 글자 (그대로 쓸 문구)

| 자리 | 문구 | 글자 수 |
|---|---|---|
| 제목 | 가르쳐 보기 | 6 |
| 부제 | 어느 쪽이 배우고 있나요 | 12 |
| 위 칸 이름 | 흔들면 세는 칸 | 8 |
| 위 칸 한 줄 | 여기엔 담기 칸이 없어요 | 12 |
| 아래 칸 이름 | 예시로 배우는 칸 | 9 |
| 버튼 | 가 담기 / 나 담기 / 다시 하기 | 4 / 4 / 5 |
| 막대 이름 | 가 / 나 | 1 |
| 장수 표시 | 가 0장 · 나 0장 | 10 |
| 관찰 물음(상시) | 예시를 더 담으면 달라지나요 | 14 |
| 개인정보(상시) | 화면에만 보이고 저장되지 않아요 | 16 |
| 안전(상시) | 손과 물건만 비춰요 | 10 |
| 되짚기(상시) | 카드 여덟 장은 종이에서 나눠요 | 16 |
| 연습 모드 알림 | 연습 모드 · 손으로 그려 담아요 | 16 |

한 화면 약 170자. 상한 250자 안이다.

**쓰지 않는 말**: 인공지능, 학습, 모델, 데이터, 센서, 변수, 만보기, 정확도.
`맞혀 보` `정답` `퀴즈` 라는 말을 쓰지 않는다(4절 금지). 비추라고만 한다.
어느 쪽이 인공지능인지 화면이 **말하지 않는다.**

---

## 6. 연습 모드

| 막히는 것 | 처리 |
|---|---|
| 카메라 권한 거부·기기 없음 | 카메라 자리에 **그리기 칸**을 띄운다. 손가락이나 마우스로 그리고 담는다. |
| 흔들기 센서 없음 | 위 칸에 `흔들기` 버튼 하나만 더 둔다(이때만). |
| 화면을 벗어남 | 카메라와 흔들림 듣기를 모두 끊는다(`visibilitychange`, `pagehide`). |

그리기 칸은 같은 24×24 특징 추출을 그대로 쓴다. **같은 개념을 겪는다.**
`진짜와 같다`고 적지 않는다. 화면에 `연습 모드`라고 표시한다.

---

## 7. 만드는 방법

외부 라이브러리 없음. CDN 없음. 저장 없음. 사진을 밖으로 보내지 않고 내려받기도 두지 않는다.

```
특징(24x24 픽셀):
  평균R, 평균G, 평균B, 평균밝기      // 0~255를 0~1로
  4x4 칸마다 평균밝기                 // 16개
  → 길이 20 배열
가중치: 앞 4개는 1.0, 뒤 16개는 0.6

판정(query):
  if (가.length === 0 || 나.length === 0) 막대를 움직이지 않는다
  dists = 모든 예시에 대해 {cls, d: 가중유클리드(query, 예시)}
  가까운 3개를 뽑는다
  sim = 1 / (d + 0.001)
  클래스별 sim 합 → 합이 1이 되게 나눈다 → 막대 길이

담기(cls):
  if (해당 클래스 20장이면 버튼을 흐리게 두고 한 줄로 알린다)
  지금 화면의 특징을 그 클래스에 넣는다
  장수 표시만 다시 그린다        // 화면 전체를 다시 그리지 않는다

흔들림: B11과 같다. 임계 16, 잠금 250ms, 상한 99.
```

- 막대 둘은 인라인 SVG `<rect>`의 `width`만 바꾼다.
- 카메라는 `facingMode: 'environment'`를 먼저 청한다(뒷면으로 물건을 비추게).
- `getTracks().forEach(t => t.stop())`로 반드시 끊는다.
- 포인트색 한 가지: 청록 `#00A39B`.

---

## 8. 난이도 점검

| 재는 것 | 상한 | 이 차시 |
|---|---|---|
| 첫 조작까지 | 30초 | **5초** (열면 흔들 수 있다) |
| 화면 수 | 3 | **1** |
| 한 화면 조작 요소 | 5 | **3** (담기 2 + 다시 하기) |
| 한 번에 바꾸는 값 | 1 | **1** (담은 장수) |
| 전체 체험 시간 | 5~10분 | **4~6분** |
| 새로 배우는 조작법 | 0 | **0** |

## 9. 디버깅

공통기준 8-2에 더해 이 차시만의 항목.

- [ ] 카메라를 거부했을 때 그리기 칸이 뜨고 담기가 그대로 된다.
- [ ] 한쪽만 담은 동안 막대가 움직이지 않는다.
- [ ] 양쪽에 한 장씩 담긴 순간부터 막대가 움직인다.
- [ ] 한쪽에만 20장을 담으면 무엇을 비춰도 그쪽으로 기운다(막지 않는다).
- [ ] 20장에서 담기 버튼이 흐려지고 까닭이 한 줄 뜬다.
- [ ] `다시 하기`가 예시와 윗 칸의 수를 모두 0으로 만든다.
- [ ] 다른 앱으로 갔다 오면 카메라 표시등이 꺼져 있다.
- [ ] 위 칸에 담기·가르치기 버튼이 **하나도 없다.**
- [ ] 화면 어디에도 어느 쪽이 인공지능인지 적혀 있지 않다.

### 앞선 판정문 (2026. 9. 11.)

> 만들지 않는다. 1~2차시의 알맹이는 사례 카드 여덟 장을 두 기준으로 나누고 까닭을 손으로
> 적는 일이다. 웹이 판정을 되돌려 주면 "더 알아봐야 함"에 표시하는 태도가 깨진다.

이 판정은 **활동2~4(카드 판정·확인 방법·고쳐 쓰기)에 대해서는 그대로 살아 있다.**
뒤집은 것은 활동1 한가운데의 5분짜리 자리뿐이다. 이 화면은 아무것도 판정해 주지 않는다.
두 칸을 만져 보게만 한다.
