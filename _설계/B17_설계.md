# B17 체험 설계 — 하나만 바꾸어 보기

## 0. 만들까 말까

**만든다.**

조건 하나만 바꾸고 나머지를 기준에 묶어 두는 일은 교실 기기로는 잘 지켜지지 않는다.
거리를 옮기는 사이 밝기가 같이 바뀌고, 다시 기준으로 돌아가기도 어렵다. 화면은
나머지 두 조건을 강제로 기준에 붙들어 두고, 바꾼 조건 하나만 단계로 움직인다.

값을 옮기는 즉시 사진과 비슷한 정도 세 줄이 다시 계산된다. 미리 적어 둔 결과가 아니라
그 자리에서 그림을 다시 그리고 재서 내놓는다. 5분 안에 끝나고 외부 호출이 없다.

## 1. 과정안 어디에 붙는가

- 블록 B17 (5~6/32차시), 5차시 **활동1·활동2 앞**.
- 학급 기기로 음성·사진을 시험하기 전에, 한 번에 하나만 바꾼다는 것이 무엇인지
  손으로 겪고 기록표의 칸 이름(바꾼 조건 하나 / 그대로 둔 조건)을 미리 잡는다.
- 교실에서 **5분**. 6차시 활동3(같은 조건으로 다시 확인)에서 한 번 더 열어도 된다.

## 2. 한 문장

거리·배경·밝기 가운데 **하나만** 단계를 올리면, 그 사진이 공·상자·컵과 얼마나 비슷한지가
바뀌고 가장 비슷한 것도 뒤집히거나 아슬아슬해진다.

## 3. 화면

**1화면.** 위에서 아래로 바꿀 조건 → 단계 → 사진과 비슷한 정도 세 줄.

| 보이는 것 | 학생이 하는 것 | 다음 |
|---|---|---|
| 조건 버튼 셋, 단계 슬라이더 하나, 지금 사진, 공·상자·컵 막대 셋 | 조건 하나를 고르고 슬라이더를 움직인다 | 없음. 같은 화면에서 조건을 바꿔 가며 되풀이한다 |

## 4. 조작과 값

- 조절하는 값은 **단계 하나**뿐이다(0~3, 처음은 0단계 = 기준).
- 조건을 바꿔 누르면 단계가 **0으로 돌아간다.** 두 조건이 동시에 기준에서 벗어날 수 없다.
- 단계가 사진에 붙는 방식

| 조건 | 0단계 | 1~3단계 |
|---|---|---|
| 거리 | 기준 크기 | 물건을 `1 − 0.18 × 단계`로 줄인다 |
| 배경 | 흰 배경 | 대각선 줄무늬를 `0.13 × 단계` 진하기로 깐다 |
| 밝기 | 기준 | 화면 전체를 `0.11 × 단계` 만큼 어둡게 덮는다 |

- 재는 방법: 사진을 16×16 회색조로 줄여, 기준 조건에서 그려 둔 공·상자·컵 세 장과
  칸별 차이를 잰다. 비슷한 정도 = `round(100 × exp(−차이 × 10))`.
- 가장 높은 줄에 `가장 비슷`을 붙인다. 1등과 2등의 차이가 **5 미만**이면 대신 `아슬아슬`을
  붙인다. 한 점 차이로 뒤집힌 결과를 단정하지 않게 하려는 것이다.
- 결과를 미리 넣어 두지 않는다. 화면을 열 때 세 장을 그려 재는 것부터 시작한다.

브라우저에서 실제로 나온 값(참고용, 화면에는 미리 적지 않는다):

| | 0단계 | 1단계 | 2단계 | 3단계 |
|---|---|---|---|---|
| 거리 | 공 100 · 가장 비슷 | **컵이 앞섬** | 아슬아슬 | 아슬아슬 |
| 배경 | 공 100 · 가장 비슷 | 공 · 가장 비슷 | 아슬아슬 | 아슬아슬 |
| 밝기 | 공 100 · 가장 비슷 | 아슬아슬 | 아슬아슬 | 아슬아슬 |

## 5. 글자 (그대로 쓸 문구)

| 자리 | 문구 | 글자 |
|---|---|---|
| 제목 | 하나만 바꾸어 보기 | 9 |
| 부제 | 나머지 조건은 기준 그대로 | 13 |
| 라벨 | 바꿀 조건 | 5 |
| 버튼 | 거리 / 배경 / 밝기 | 2·2·2 |
| 단계 표시 | 0단계 … 3단계 | 3 |
| 슬라이더 양 끝 | 기준 / 많이 | 2·2 |
| 막대 이름 | 공 / 상자 / 컵 | 1·2·1 |
| 표시 | 가장 비슷 / 아슬아슬 | 5·4 |
| 버튼 | 처음으로 | 4 |
| 관찰 | 무엇을 바꾸니 결과가 달라졌나요? | 17 |
| 관찰 | 같은 조건으로 한 번 더 해 보세요 | 17 |

화면 전체 **120자** (브라우저에서 실제로 센 값).

## 6. 연습 모드

카메라·마이크를 쓰지 않는다. 얼굴은 아예 다루지 않고 책상 위 물건만 그린다
(공통기준 6-4). 사진은 그 자리에서 캔버스에 그리므로 바깥에서 받아 오는 파일이 없고
인터넷을 끊어도 같게 열린다. 막힐 것이 없어 따로 연습 모드를 두지 않았다.

이 화면은 실제 인식 기능이 아니다. 조건 하나를 바꾸면 재는 값이 어떻게 움직이는지
보는 자리이며, 활동1·활동2의 학급 기기 시험을 대신하지 않는다.

## 7. 만드는 방법

외부 라이브러리 없음. CDN 없음. 바닐라 JS + `<canvas>` 한 파일.

```
init():
  공·상자·컵을 기준 조건으로 각각 그려 16x16 회색조 특징으로 저장 (기준 자료)

update():
  공을 지금 조건·단계로 다시 그린다
  16x16 회색조 특징을 뽑는다
  세 기준 자료와 칸별 차이(RMS)를 재서 비슷한 정도를 셋 낸다
  막대 너비와 숫자만 고쳐 쓴다 (화면을 다시 그리지 않는다)
  1등과 2등 차이가 5 이상이면 '가장 비슷', 아니면 '아슬아슬'

조건 버튼 누름 -> 조건 바꾸고 단계를 0으로 -> update()
슬라이더 움직임 -> 단계만 바꾸고 -> update()
처음으로       -> 거리·0단계로 -> update()
```

`getImageData`를 쓰므로 `willReadFrequently: true`로 컨텍스트를 연다. 바깥 그림을 얹지
않아 캔버스가 오염되지 않고 `file://`에서도 읽힌다.

## 8. 난이도 점검

| 재는 것 | 상한 | 이 차시 |
|---|---|---|
| 첫 조작까지 | 30초 | 즉시 (조건 버튼이 화면 위쪽) |
| 화면 수 | 3 이하 | 1 |
| 한 화면의 조작 요소 | 5 이하 | 5 (버튼 3 + 슬라이더 1 + 처음으로) |
| 한 번에 바꾸는 값 | 1 | 1 (조건을 바꾸면 단계가 0으로 돌아간다) |
| 전체 체험 시간 | 5~10분 | 5분 |
| 새로 배우는 조작법 | 0 | 0 (누르기·끌기, 슬라이더는 눌러서도 움직인다) |

## 9. 디버깅

공통기준 8-2 점검표에 이 차시에서 더 본 것:

- 조건을 바꿔 눌렀을 때 단계가 반드시 0으로 돌아가는가 (두 조건이 동시에 어긋나면 안 된다).
- 슬라이더를 양 끝(0단계·3단계)으로 몰아도 막대와 숫자가 깨지지 않는가.
- 밝기 3단계처럼 셋 다 낮게 나올 때 막대가 비어 보이지 않고 숫자가 읽히는가.
- 캔버스에 글자가 없으므로 `role="img"`와 `aria-label`이 붙어 있는가.
- 화면을 열 때 값이 미리 적혀 있지 않고 계산으로 채워지는가.

### 검사 결과 (2026. 9. 11.)

```
python _tools/run_all.py B16 B17
문법 (node --check)   B17 OK
감사 (공통기준)        B17 OK  — 0건
헤드리스 (Edge)        B17 OK  — 0건
```

- 손으로 본 것: 조건 3 × 단계 4 = 열두 자리를 모두 열어 값을 받아 적었다(4절 표).
  값이 어긋나거나 화면이 비는 자리는 없었다. `처음으로` 뒤 기준 상태로 돌아온다.
  폭 360px 가로 넘침 없음, Tab 이동 가능, 콘솔 오류 0건.
- 남은 것: 실제 태블릿(터치)과 저사양 크롬북에서는 확인하지 못했다. 캔버스 읽기가
  매 단계마다 일어나므로 아주 느린 기기에서는 슬라이더가 뻑뻑할 수 있다.
