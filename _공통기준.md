# 에듀다움 체험 웹 — 공통 설계 기준

작성일: 2026. 9. 11.
대상 학생: 초등 5·6학년
본보기: `https://github.com/kwonjungu/ebs` (로컬 클론 `C:\Users\user\ebs-fix`)
만들 것: 에듀다움 학교자율시간 과정안 28블록 가운데 **웹 체험이 필요한 차시만** 고른 활동 웹

이 문서는 차시별 설계서를 쓰는 모든 사람이 먼저 읽는다. 차시별 설계서는 이 기준을
다시 설명하지 않고, 이 기준과 **다르게 하는 부분만** 적는다.

---

## 1. 이 웹이 하는 일과 하지 않는 일

**하는 일.** 교실에서 준비물 없이 30초 안에 만져 볼 수 있는 조각 하나를 준다.
학생이 값을 바꾸고 결과가 달라지는 것을 눈으로 본다.

**하지 않는 일.** 수업을 대신하지 않는다. 마이크로비트·엔트리·실제 측정을 대체하지
않는다. 과정안에 있는 내용을 웹에 옮겨 적지 않는다.

에듀다움 과정안은 실물 교구와 실제 측정을 핵심으로 삼는다(`에듀다움_피지컬교구_내용업데이트_설계안.md`).
웹은 그 앞이나 뒤에 붙는 **짧은 보조 도구**다. 기기가 없는 학급의 대체 수단으로 쓸
수는 있으나, 실물 체험과 같다고 적지 않는다.

---

## 2. 이 활동이 정말 필요한가 — 필요성 판정

**기본값은 "만들지 않는다"이다.** 아래 네 가지를 모두 넘길 때만 만든다.

1. **웹이 아니면 못 하는가.** 종이·칠판·실물로 더 잘 되는 것은 만들지 않는다.
   예: 명령어 카드 놀이, 순서도 그리기, 역할 나누기, 전시·발표는 만들지 않는다.
2. **값을 바꾸면 결과가 바뀌는가.** 학생이 조절할 것이 있고 그 결과가 즉시 보이는가.
   읽기만 하는 화면, 설명만 있는 화면은 만들지 않는다.
3. **40분 수업에서 5~10분 안에 끝나는가.** 그보다 길면 수업을 잡아먹는다.
4. **교실 기기로 되는가.** 설치 없이 브라우저만으로, 외부 서버·API 키 없이 되는가.

네 가지를 다 넘겨도, **그 차시에서 이미 실물로 하는 일을 웹으로 한 번 더 하는 것**이면
만들지 않는다. 웹은 실물 앞의 예상이나 실물 뒤의 견주기에 쓴다.

판정 결과는 차시별 설계서 맨 앞에 **만든다 / 만들지 않는다**로 적고 까닭을 두 줄로 쓴다.
만들지 않기로 한 것도 반드시 적는다. 빠뜨린 것과 일부러 뺀 것을 구별해야 한다.

---

## 3. 어렵지 않은가 — 난이도 상한

| 재는 것 | 상한 |
|---|---|
| 첫 조작까지 | 안내를 읽지 않고 **30초 안** |
| 화면 수 | **3화면 이하** (도입 없이 바로 조작 화면이 기본) |
| 한 화면의 조작 요소 | **5개 이하** |
| 한 번에 바꾸는 값 | **1개** |
| 전체 체험 시간 | **5~10분** |
| 새로 배우는 조작법 | **0개** (누르기·끌기·고르기만 쓴다) |

넘기면 활동을 쪼개지 말고 **덜어 낸다.** 기능을 더해서 어려워졌다면 그 기능이 잘못된 것이다.

**어려움의 신호** — 아래가 하나라도 있으면 다시 설계한다.
- 무엇을 눌러야 할지 화면만 보고 모르겠다.
- 설명을 읽어야 조작할 수 있다.
- 잘못 누르면 되돌릴 수 없다.
- 결과가 왜 그렇게 나왔는지 화면에 아무 단서가 없다.

---

## 4. 글자와 개념 설명을 줄인다

**개념은 글로 설명하지 않고 조작으로 겪게 한다.**

| 칸 | 글자 상한 |
|---|---|
| 제목 | 20자 |
| 부제 | 35자, 한 줄 |
| 버튼 | 10자 |
| 안내 한 줄 | 30자 |
| 결과 문구 | 40자 |
| 한 화면 전체 | **250자** (버튼·라벨 포함) |

**금지**
- 개념 설명 문단. "~란 ~입니다" 꼴의 정의문.
- 학습 목표·성취기준을 화면에 적는 것. 그것은 교사 문서에 있다.
- 접어 둔 긴 설명(`details`) 안에 개념 설명을 숨겨 두는 것. 숨겨도 글자는 글자다.
- 퀴즈로 개념을 확인하는 마무리 화면. 퀴즈를 넣지 않는다.
- 배지·칭찬 문구를 여러 줄로 붙이는 것.

**대신 쓰는 것**
- 값이 바뀌면 숫자·막대·그림이 즉시 바뀌는 화면.
- 같은 것을 두 방식으로 나란히 보여 주는 비교 화면.
- 한 줄짜리 관찰 물음. 예: `무엇을 바꾸니 결과가 달라졌나요?`

용어는 화면에 **딱 한 번, 라벨로만** 띄운다. 뜻풀이를 붙이지 않는다.
예: 막대 위에 `소리 크기`라고만 적는다. "소리 크기란 …"이라고 쓰지 않는다.

---

## 5. 직관성과 사용성 — 모든 차시 공통

### 5-1. 첫 화면
- 열자마자 **조작할 것이 화면 가운데** 있다. 시작 버튼으로 한 단계 미루지 않는다.
- 무엇을 하는 화면인지 제목 한 줄로 끝낸다.
- 스크롤 없이 조작 영역이 보인다(세로 800px 기준).

### 5-2. 조작
- 손가락으로 누를 것은 **최소 44×44px**, 간격 8px 이상.
- 주된 조작은 **하나**다. 슬라이더면 슬라이더 하나, 버튼이면 버튼 하나가 눈에 띈다.
- 끌기(drag)를 쓰면 누르기로도 되게 한다. 끌기만 되는 조작은 넣지 않는다.
- **되돌리기가 항상 있다.** 되돌리기 버튼이 없으면 다시 하기 버튼이라도 둔다.
- 확인 대화상자를 띄우지 않는다. 되돌릴 수 있으면 그냥 실행한다.

### 5-3. 상태가 늘 보인다
- 지금 값이 얼마인지 화면에 숫자로 보인다.
- 기다리는 중이면 기다린다고 보인다(회전 표시 + 한 줄).
- 안 되는 버튼은 흐리게 두고, 왜 안 되는지 한 줄로 알린다.
- 결과는 **바뀐 부분만** 강조한다. 화면 전체를 다시 그리지 않는다.

### 5-4. 실패를 막는다
- 잘못 누를 수 있는 자리를 만들지 않는다. 위험한 동작 자체를 두지 않는다.
- 입력값은 범위를 정해 두고 벗어날 수 없게 한다. 숫자 직접 입력보다 슬라이더·버튼.
- 학생이 채울 칸은 비워 둔다. 예시 값을 미리 채워 두지 않는다.

### 5-5. 손에 맞춘다
- 태블릿 가로·세로, 노트북 모두에서 쓸 수 있게 한다. 화면이 좁으면 세로로 쌓는다.
- 글자는 16px 이상. 학생 기기의 기본 확대를 방해하지 않는다.
- `prefers-reduced-motion`을 존중해 애니메이션을 끈다.
- `:focus-visible` 테두리를 남긴다. 키보드로도 순서대로 짚을 수 있게 한다.
- 색으로만 구별하지 않는다. 모양이나 글자를 함께 쓴다.

---

## 6. 규격

### 6-1. 파일과 경로
```
에듀다움_체험웹/
  index.html          허브(차시 목록)
  B07/index.html      블록 하나에 폴더 하나, 단일 파일
  assets/             공용 그림 (있을 때만)
```
- **한 차시는 한 파일이다.** 외부 js·css 파일로 쪼개지 않는다.
- 서버·API 키·로그인·데이터베이스를 쓰지 않는다. 정적 호스팅에서 그대로 열린다.
- 저장이 필요하면 `localStorage`만 쓴다. 학생 이름을 받지 않는다.

### 6-2. 바깥에서 가져오는 것
- 허용: jsdelivr CDN, MediaPipe 공식 모델 스토리지.
- 금지: 그 밖의 외부 호출, 추적 스크립트, 광고, 폰트 서버 의존(폰트는 시스템 글꼴).
- CDN을 쓰면 **못 받아 올 때의 연습 모드**를 반드시 함께 만든다(7절).

### 6-3. 보이는 모양
ebs의 `DESIGN_SYSTEM.md`를 따른다. 요점만 옮기면,
- 크림 바탕 `#FBF7EC` + 먹색 `#141414` 아웃라인 2.5~3px + 하드 그림자 `5px 5px 0`.
- 차시마다 포인트색 **한 가지**. 파스텔 여러 색을 쓰지 않는다.
- **이모지를 아이콘으로 쓰지 않는다.** 아이콘은 인라인 SVG로 그린다.
- 둥근 그림자 카드를 반복하지 않는다.

### 6-4. 개인정보와 안전
- 카메라·마이크를 쓰면 화면에 상시 한 줄: `화면에만 보이고 저장되지 않아요`.
- 얼굴을 다루는 활동은 만들지 않는다. 손·물건·소리만 쓴다.
- 촬영물을 밖으로 보내지 않는다. 내려받기 기능도 기본으로 두지 않는다.
- 학생끼리 견주는 점수판·순위를 만들지 않는다.

---

## 7. 안 될 때를 먼저 만든다 — 연습 모드

교실에서 카메라·마이크·CDN은 **자주 막힌다.** 그러므로 연습 모드는 덤이 아니라 기본이다.

| 막히는 것 | 처리 |
|---|---|
| 권한 거부·기기 없음 | 바로 연습 모드로 넘어간다. 한 줄로 알린다. |
| CDN·모델 못 받음 | 15초 기다린 뒤 연습 모드. |
| 성능이 느림 | 처리 주기를 100ms로 늦춘다. |
| 화면을 벗어남 | 카메라·마이크를 끈다(`visibilitychange`, `pagehide`). |

**연습 모드도 같은 개념을 겪게 한다.** 미리 넣어 둔 자료나 직접 그리기로 대신하되,
"진짜와 같다"고 적지 않는다. 화면에 `연습 모드`라고 표시한다.

---

## 8. 디버깅 절차 — 내기 전에 반드시 돌린다

### 8-1. 문법 검사 (ebs와 같은 방식)
`<script>` 블록을 뽑아 `node --check`로 검사한다. 한 파일에 여러 블록이면 전부 본다.

```
python _tools/jscheck.py B07        # _chk_B07_0.mjs … 를 만들고 node --check
```
`OK`가 아니면 고친다. 검사기를 고쳐서 통과시키지 않는다.

### 8-2. 손으로 보는 점검표 — 전부 통과해야 한다

**켜기**
- [ ] 파일을 두 번 눌러 브라우저로 열었을 때 콘솔 오류가 0건이다.
- [ ] 새로 고쳐도 처음 상태로 제대로 돌아온다.
- [ ] 인터넷을 끊고 열었을 때 연습 모드로 넘어간다.

**조작**
- [ ] 안내를 읽지 않고 30초 안에 첫 조작이 된다(직접 재 본다).
- [ ] 주된 조작을 10번 빠르게 반복해도 값이 어긋나지 않는다.
- [ ] 버튼을 두 번 빠르게 눌러도 같은 일이 두 번 일어나지 않는다.
- [ ] 되돌리기나 다시 하기로 처음 상태가 된다.
- [ ] 값의 양 끝(최소·최대)에서 화면이 깨지지 않는다.

**권한**
- [ ] 카메라·마이크를 **거부**했을 때 연습 모드로 넘어간다.
- [ ] 허용했다가 도중에 껐을 때 멈추지 않고 안내가 뜬다.
- [ ] 화면을 벗어나면 카메라·마이크가 꺼진다(표시등으로 확인).

**화면**
- [ ] 폭 360px에서 가로 스크롤이 생기지 않는다.
- [ ] 태블릿 가로·세로에서 조작 영역이 한 화면에 들어온다.
- [ ] 키보드 `Tab`으로 모든 버튼에 닿고 테두리가 보인다.
- [ ] 한 화면 글자 수가 250자를 넘지 않는다(세어 본다).

**내용**
- [ ] 화면에 개념 정의문이 없다.
- [ ] 학생이 채울 칸에 예시 값이 미리 들어 있지 않다.
- [ ] 과정안에 없는 도구·용어를 새로 만들지 않았다.
- [ ] 개인정보 문구가 카메라·마이크 화면에 상시로 보인다.

### 8-3. 흔한 함정 (ebs에서 실제로 겪은 것)
- 화면을 통째로 다시 그리면 **입력 중이던 값이 사라진다.** 바뀐 부분만 고쳐 그린다.
- 파일 고르기 창이 열리고 닫히면서 화면이 다시 그려져 내용이 날아간다.
- 버튼 글자를 코드가 바꿀 때 아이콘까지 같이 지워진다. 아이콘은 글자 밖에 둔다.
- 카메라를 끄지 않고 화면을 옮기면 표시등이 계속 켜져 있다.
- 여러 번 눌러 같은 요청이 쌓인다. 진행 중에는 잠근다.

### 8-4. 보고
설계서 끝에 **검사 결과**를 적는다. 통과·미통과를 그대로 적고, 못 고친 것은
무엇이 남았는지 쓴다. 통과했다고만 적지 않는다.

---

## 9. 차시별 설계서 형식

파일 이름: `_설계/BXX_설계.md`

```
# BXX 체험 설계 — <활동 이름>

## 0. 만들까 말까
만든다 / 만들지 않는다 + 까닭 두 줄 (2절 네 가지 기준에 비추어)
만들지 않기로 했으면 여기서 끝낸다.

## 1. 과정안 어디에 붙는가
블록·차시·활동 이름, 실물 활동의 앞인지 뒤인지, 교실에서 몇 분

## 2. 한 문장
학생이 무엇을 바꾸면 무엇이 달라지는지 한 문장

## 3. 화면 (3화면 이하)
화면마다: 보이는 것 / 학생이 하는 것 / 다음으로 넘어가는 조건

## 4. 조작과 값
조절하는 값 하나, 범위, 기본값, 결과에 어떻게 반영되는지

## 5. 글자 (그대로 쓸 문구)
제목·부제·버튼·안내·결과 문구를 글자 수와 함께. 4절 상한 안에서.

## 6. 연습 모드
무엇이 막혔을 때 어떻게 대신하는지

## 7. 만드는 방법
쓰는 기술, CDN 주소, 핵심 로직 의사코드 (외부 라이브러리 없으면 없다고 적는다)

## 8. 난이도 점검
3절 표에 실제 값을 채운다

## 9. 디버깅
8-2 점검표에서 이 차시에만 해당하는 항목을 덧붙인다
```

---

## 10. 하지 말 것

1. 과정안의 성취기준·차시·도구를 바꾸지 않는다. 웹은 과정안을 따른다.
2. 실물 교구 체험을 웹으로 대신했다고 적지 않는다.
3. 점수·등수·학생 이름을 다루지 않는다.
4. 센서가 달렸다는 이유로 인공지능이라고 설명하지 않는다.
5. 결과 수치를 미리 정해 두지 않는다.
6. 이 기준과 다르게 하려면 설계서에 **까닭을 적고** 다르게 한다. 조용히 어기지 않는다.

---

## 11. 몸으로 하는 차시 — 센서와 미니 이미지 모델

2026. 9. 17. 덧붙임. 만드는 순서는 `_정교화_프로세스.md`를 따른다.

### 11-1. 센서를 쓸 수 있는 자리

과정안 5학년은 **마이크로비트**를 쓰고(흔들림·버튼·소리), 6학년은 **소리·온도·카메라**를 다룬다.
태블릿 센서는 그 자리에만 쓴다. **과정안에 없는 감각을 새로 들여오지 않는다.**

| 과정안이 쓰는 것 | 태블릿에서 대신 만져 보는 것 | 쓰는 자리 |
|---|---|---|
| 마이크로비트 흔들림 | `devicemotion` 가속도 | 전송 **전**에 무엇을 세는지 확인 |
| 마이크로비트 소리·6학년 음성 | `getUserMedia` 마이크 | 소리가 수로 바뀌는 것을 봄 |
| 6학년 이미지 인식·분류 모델 | 카메라 + 미니 이미지 모델 | 예시를 가르쳐 보고 틀린 까닭을 찾음 |

**대신하지 않는다.** 마이크로비트 전송·복도 실측·엔트리 모델 학습은 그대로 한다.
화면에 `기기로 다시 해 봐요` 꼴의 한 줄을 상시로 둔다.

### 11-2. 재미는 규칙을 어기지 않고 만든다

- **점수판·등수·이름을 만들지 않는다**(6-4 그대로). 재미는 *값이 올라가는 것*과
  *표시가 갈리는 것*과 *해 보고 싶은 딴짓*에서 나온다.
- 과정안이 짚어 둔 딴짓은 **막지 않고 열어 둔다.** 예: B11 활동10의
  "걷지 않고 기기만 흔들었을 때"는 웹에서도 그대로 올라가야 한다. 막으면 그 차시가 없어진다.
- 목표에 닿으면 화면이 눈에 띄게 달라진다. 칭찬 문구를 여러 줄 붙이지 않는다(4절).

### 11-3. 센서를 쓸 때의 규격

- **첫 화면에서 바로 조작된다.** 권한 버튼 뒤에 조작을 숨기지 않는다.
  누르기로 하는 길(연습 모드)을 처음부터 함께 두고, 센서는 되면 저절로 붙는다.
- iOS는 `DeviceMotionEvent.requestPermission`이 필요하다. 없는 기기는 바로 붙이고,
  필요한 기기만 켜기 버튼을 보인다.
- 흔들림은 **가속도 차이의 크기**로 세고, 0.2초 안의 되풀이는 한 번으로 본다.
- 화면을 벗어나면 센서·카메라·마이크를 끊는다(`visibilitychange`, `pagehide`).
- 몸을 쓰는 활동에는 안전 한 줄을 상시로 둔다. 예: `두 손으로 잡고 흔들어요`.
- 미니 이미지 모델은 기기 안에서만 돈다. 사진을 밖으로 보내지 않고 내려받기도 두지 않는다.
  얼굴은 다루지 않는다. **손과 물건만** 쓴다(6-4).
