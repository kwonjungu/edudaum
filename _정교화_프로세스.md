# 정교화 프로세스 — 한 차시를 내보내기까지

작성일: 2026. 9. 17.

`_공통기준.md`가 **무엇을 지킬지**를 정한다면, 이 문서는 **어떤 순서로 만들지**를 정한다.
한 차시는 아래 네 단계를 차례로 지나며, 단계마다 커밋을 남긴다. 단계를 건너뛰지 않는다.

---

## 1단계 · 게임 디자인 (설계)

과정안 원문에서 **핵심 요소**를 먼저 뽑는다. 요소는 활동 이름이 아니라 **학생이 실제로 하는 일**이다.

> 예) B11의 핵심 요소 = ① 값을 담는 주머니 ② 흔들림 한 번에 1 ③ 되돌리기 ④ 목표에 따라 갈리는 표시
> ⑤ 걷지 않고 흔들어도 올라간다(과정안 활동10)

그 요소 가운데 **몸으로 하면 재미있어지는 것**을 고른다. 태블릿 센서(가속도·기울기·나침반·마이크)와
미니 이미지 모델은 여기서만 쓴다. 센서를 쓸 자리가 없으면 쓰지 않는다.

산출: `_설계/BXX_설계.md`. 형식은 공통기준 9절. 구현자가 더 생각하지 않아도 되도록
**화면·값·문구·의사코드까지** 적는다.

## 2단계 · 구현 (잡일)

설계서를 그대로 옮긴다. 판단이 필요하면 옮기지 말고 1단계로 되돌린다.
구현은 보조 에이전트에게 맡길 수 있다. 맡길 때는 설계서 전문과 `_공통기준.md`를 함께 준다.

내기 전에 `python _tools/run_all.py BXX`가 **0건**이어야 한다. 검사기를 고쳐서 통과시키지 않는다.

## 3단계 · 교사 검토 → 커밋

교사가 이 화면을 수업에 넣을 수 있는지 본다. **하나라도 걸리면 내지 않는다.**

- [ ] 과정안의 어느 활동 **앞**이나 **뒤**에 붙는지 한 줄로 말할 수 있다.
- [ ] 과정안이 실물로 하는 일을 화면이 대신하지 않는다.
- [ ] 과정안에 없는 용어·도구를 새로 만들지 않았다.
- [ ] 결과 수치를 미리 정해 두지 않았다. 학생 기기가 실제로 잰다.
- [ ] 판단이 갈리는 자리에서 화면이 정답을 돌려주지 않는다.
- [ ] 센서가 달렸다는 까닭으로 인공지능이라고 말하지 않는다.
- [ ] 개인정보 문구·안전 문구가 상시로 보인다.
- [ ] 기기가 없거나 권한이 막힌 학급도 같은 개념을 겪는다.

커밋 메시지: `BXX 교사 검토: <고친 것>`

## 4단계 · 재미 검토 → 커밋

아이들이 10분을 쓰고 싶어 하는지 본다. **셋 이상 비면 다시 설계한다.**

- [ ] 열고 **10초 안에** 몸을 움직이게 된다. 읽을 것이 앞을 막지 않는다.
- [ ] 한 번 하고 **또 하고 싶어지는 까닭**이 있다(값이 올라감·표시가 바뀜·해 보고 싶은 딴짓).
- [ ] 딴짓할 자리를 막지 않았다. 과정안이 짚는 딴짓이면 오히려 열어 둔다.
- [ ] 반응이 **즉시** 온다(100ms 안에 숫자·그림이 움직인다).
- [ ] 끝이 있다. 목표에 닿으면 화면이 눈에 띄게 달라진다.
- [ ] 소리를 지르게 되는 순간이 한 번은 있다.
- [ ] 순위·등수를 만들지 않고도 재미가 선다.

커밋 메시지: `BXX 재미 검토: <고친 것>`

---

## 단계마다 따로 커밋하는 까닭

교사 검토에서 고친 것과 재미 검토에서 고친 것이 섞이면, 나중에 "왜 이렇게 됐지"를 되짚을 수 없다.
교실에서 문제가 생겼을 때 어느 검토가 놓친 것인지 커밋으로 가른다.
